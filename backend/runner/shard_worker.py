"""Shard worker — the reconciliation tick (backend-architecture §8.3).

Phase 5 replaces this stub with lease/reconcile logic: desired-state poll, paced
generation, ledger append, chaos transform, publish, checkpoint.
"""
