"""DataForge engine — framework-free generation packages (backend-architecture §4).

Pure Python: zero Django, DRF, Celery, Channels, redis, confluent_kafka, or
psycopg imports (rule BE-ENG-1; import-linter contract 2 is CI-blocking).
"""
