"""Behavior engine: state-machine interpreter, entity pools, arrival processes, virtual
clock, checkpoint codec (behavior-engine spec). Phase 4 replaces this stub with the
behavior implementation.
"""
