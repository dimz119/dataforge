import { PlaceholderPage } from '../../../shared/ui/PlaceholderPage';

/** Phase 1 routing-skeleton placeholder. Phase 7 replaces this with the working page. */
export function InstanceConfigPage() {
  return <PlaceholderPage group="Scenarios" page="Instance configuration" phase={7} />;
}
